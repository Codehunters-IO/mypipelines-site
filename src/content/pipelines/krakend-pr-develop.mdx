---
title: "Krakend Pr Develop"
slug: "krakend-pr-develop"
stack: "krakend"
kind: "caller"
type: "misc"
triggers: ["pull_request"]
description: "Template: PR to develop validation Copy to: .github/workflows/pr-develop.yml Triggers on pull requests targeting develop branch Runs: commit-lint + config validation + audit"
sourcePath: "templates/krakend-pr-develop.yml"
---

```yaml title="templates/krakend-pr-develop.yml"
# Template: PR to develop validation
# Copy to: .github/workflows/pr-develop.yml
#
# Triggers on pull requests targeting develop branch
# Runs: commit-lint + config validation + audit

name: PR Validation

on:
  pull_request:
    branches:
      - develop

jobs:
  validate:
    uses: <org>/ci-templates/.github/workflows/krakend-main-pipeline.yml@main
    with:
      run_commit_lint: true
      run_build: true
      run_test: true
      audit_threshold: 0
    secrets: inherit
```
